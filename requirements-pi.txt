# Raspberry Pi (ARM64) runtime dependencies. Install into a Python 3.12 venv
# with uv - see PI_SETUP.md. rapidocr-onnxruntime is pinned to 1.4.4 because it
# uses the accurate PP-OCRv4 model AND requires Python <3.13; the others are left
# unpinned so pip resolves whatever ARM64 wheels exist for your Python version
# (exact pins validated on x86 don't always have matching Pi wheels).
numpy
opencv-python
onnxruntime
rapidocr-onnxruntime==1.4.4
pandas
rapidfuzz
tqdm
psycopg2-binary        # PostgreSQL driver (Phase 10 - local event store; see POSTGRES_SETUP.md)
